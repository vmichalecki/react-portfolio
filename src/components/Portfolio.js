import React from 'react';

function Portfolio() {
    const message = 'what the shit is this?'
    const message2 = 'am i doing this right?'

    return (
        <div>
            <p>{message}</p>
            <h4>{message2}</h4>
        </div>
    );
}

export default Portfolio;